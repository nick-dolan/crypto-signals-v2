# План

- Утвердили набор признаков и решили собирать данные раз в час с Binance USDT perpetual.
- Расширили исходный список до 250 монет без stablecoins.
- Рынки связываем с монетами по уникальному идентификатору; контракты вроде `1000PEPE` не берём.
- Первый шаг теперь оставляет только монеты с активным Binance USDT perpetual, сохраняя порядок общего рейтинга.
- Ранее живым запросом нашли первые 100 монет с полным набором данных.
- Второй шаг теперь отбирает готовые монеты и сохраняет историю каждой принятой монеты в отдельный файл, не накапливая всё в памяти.
- При проверке и сохранении берём только завершённые часы; текущий незакрытый час отбрасываем.
- Принимаем только монеты с полной непрерывной историей; более короткое окно оставили только для Volume Delta.
- Монеты, у которых обязательного показателя нет вообще, запоминаем и временно не берём в следующие запуски.
- Пустая категория разрешена.
- Дальше: заново проверить итоговые 100 монет живым запуском.

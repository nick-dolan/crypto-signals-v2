# План

- Утвердили набор признаков и решили собирать данные раз в час с Binance USDT perpetual.
- Собрали и утвердили список монет, за которыми будем следить.
- Загрузили для этих монет полную историю всех нужных данных.
- Добавили отдельную загрузку общей капитализации и основных сегментов рынка.
- Зафиксировали единое время сбора, чтобы все ряды заканчивались одним часом.
- Рассчитали все признаки и собрали компактный профиль для каждой монеты.
- Добавили предварительный отбор, который сохраняет разные типы сильных сигналов и сокращает список монет.
- Собрали компактную и понятную таблицу признаков для агента.
- Подключили официальный GitHub Copilot SDK для работы с агентом.
- Подготовили промпт, который оценивает вероятность сильного движения, выделяет пять самых интересных монет и возвращает понятный JSON.
- Для анализа выбрали GPT-6-Astra со стандартным контекстом и высоким уровнем рассуждения.
- Добавили седьмой шаг: агент получает подготовленные признаки и при необходимости уточняет их по сырым данным.
- Точные значения признаков монеты и общего состояния рынка теперь подставляются автоматически, поэтому округления в ответе агента не ломают анализ.
- В результат седьмого шага добавили число проверенных монет, причины за и против для лучших кандидатов и ссылки на графики для всех оценок.
- В временных исключениях теперь сохраняем, каких данных не было и по какой причине.
- Монеты без Social-данных теперь сохраняем и явно помечаем.
- Добавили получение списка новостей TradingView и текста доступных статей.
- Добавили восьмой шаг: сохраняем только пять лучших кандидатов и добавляем им до трёх свежих неповторяющихся новостей.
- Подключили TwitterAPI.io для сбора свежих публикаций.
- Добавили девятый шаг: для лучших кандидатов загружаем публикации за последние сутки.
- Добавили десятый шаг: для каждой лучшей монеты отдельно объединяем исходное объяснение со свежим информационным фоном.
- Проверили десятый шаг на реальных данных: Gemini 3.7 Flash обработала пять монет и добавила им информационный фон.
- В результате десятого шага оставляем объединённое объяснение без сырых новостей и публикаций.
- Описали признаки, которые оценивают близкие зоны потенциальных ликвидаций и направление возможного каскада.
- Решили сначала дать агенту более надёжные признаки свежего Open Interest рядом с ценой, без попытки угадывать точные ликвидации.
- Уже зрелые пампы исключаем до анализа, а итоговая пятёрка ориентирована на ранние стадии нового движения.
- Подготовили подключение к Binance для загрузки новых данных.
- Добавили самостоятельный HTML-отчёт после анализа: состояние рынка, лучшие монеты, недельные свечи, объём и Open Interest, объяснения сигналов.
- Перенесли отчёт в конец, на одиннадцатый шаг: для лучших монет показываем дополненное объяснение, новости и публикации Twitter.
- Добавили обновление графика выбранной монеты по кнопке: свежие свечи, объём и Open Interest, неподвижная отметка исходного среза. Исходный отчёт и оценки не меняются.
- Отчёты сохраняем отдельно, с датой и временем GMT+3 в имени: каждый запуск оставляет новый файл, старые отчёты не удаляются.
- Монеты с неполной историей временно пропускаем до накопления нужных часов: срок повторной проверки зависит от пропусков, а не от фиксированного ожидания.
- Вверху отчёта показываем общий фон альтрынка: рост, снижение или смешанную картину, с цветом и понятными основаниями оценки.
- Фон альтрынка теперь считаем вместе с остальными признаками: агент и отчёт получают один готовый результат.
- Добавили близость к границам диапазона, текущий funding и насыщенность открытыми позициями; отмечаем давление на границы и условия для сквиза вверх или вниз.
- Исправили расчёт Premium и определение зрелой базы; поздние падения теперь исключаем так же, как поздние пампы.
- Убрали отрицательные предупреждения из причин отбора, а непроверенное отношение ликвидаций к OI — из отбора и данных агента. Для сквиза учитываем знак funding.
- Разрешили итоговый список от нуля до пяти монет: без убедительных ранних сигналов он остаётся пустым. Проверки проходят, включая пустой список на всех последующих шагах.
- Проверили расчёты на сохранённом срезе: все 242 профиля рассчитались, отбор оставил 25 кандидатов. Правило позднего падения исключило 145 монет.
- Исправили остановку сбора из-за одинаковых мест разных монет в рейтинге.
- Добавили оценку устойчивой силы монеты: как она переносит падения рынка, участвует в росте и сохраняет преимущество сейчас. Агент получает её отдельно; правила отбора не меняли.
- Проверили новую оценку на сохранённых данных всех 250 монет: ошибок нет, прежние признаки и отбор не изменились. По стартовым условиям у 57 монет сила появляется, устойчивых лидеров пока нет.
- Сократили блок устойчивой силы для агента с двадцати до восьми основных признаков; полный расчёт сохранили для проверки.
- В HTML-отчёте выделили устойчивую силу отдельной карточкой: понятный статус, историческая и текущая оценки. Отсутствие данных явно показываем.
- Данные для агента теперь сохраняем в удобном для чтения виде, но отправляем по-прежнему компактно; настроили Zed для длинных строк.
- Разделили данные каждой монеты для агента на понятные тематические блоки, сохранив все признаки и прежние правила анализа.
- Добавили отдельный сбор трендовых монет CoinGecko и их трендовых категорий; подтверждённые совпадения с нашими монетами передаём агенту.
- Проверили на реальных данных: нашли десять совпадений, прежний отбор монет не изменился.
- В HTML-отчёте помечаем трендовые монеты CoinGecko и показываем их трендовые категории; отсутствие пересечений обозначаем отдельно.
- В HTML-отчёте по умолчанию сначала показываем топ агента, остальные монеты — по вероятности движения.
- Убрали прогноз роста или падения из анализа и HTML-отчёта: оцениваем только вероятность сильного движения, надёжность оценки и причины за и против.
- Утвердили идею сигнала догоняющего движения: замечаем свежий рост соседей по категории и ищем монеты, которые ещё не начали двигаться. Факты считаем программно и передаём общему агенту, без отдельного запроса.
- Подготовили промпт для отдельного исследования близких монет и конкурентов: связи проверяем по источникам, результат сохраняем отдельно для проверки и последующих обновлений.
- Проверили первый справочник связей: состав и структура корректны, но многие монеты остались без соседей; нашли возможные пары для дополнительной проверки.
- Уточнили промпт: отдельно ищем конкурентов и монеты с общей инвестиционной темой, перепроверяем отсутствие соседей и сравниваем новый результат с прошлой попыткой.
- Проверили повторное исследование: связи стали содержательнее и охватывают почти все монеты; структура и даты согласованы. Отдельные сравнения остаются широкими, поэтому начинаем с дополнительного контекста, а не самостоятельного сигнала.
- Приняли рабочий справочник и подключили свежие движения прямых соседей к общему агенту: учитываем возраст импульса и реакцию самой монеты, без отдельного запроса и изменения отбора.
- Проверили на сохранённых данных 250 монет: прежние признаки и список из 25 кандидатов не изменились. Все тесты проходят; отсутствие данных о соседях не считаем отсутствием сигнала.
- Добавили независимый радар соседей: отдельно находим расхождения и передаём их отдельному агенту, без остальных сигналов и без вмешательства в основной выбор монет.
- Наблюдения сохраняем отдельным JSON-отчётом; основной HTML сохранили и перенесли в конец. Ошибка радара не мешает его созданию.
- На сохранённом срезе нашли 23 монеты с расхождениями среди 249 загруженных; проверки прошли. Живой запрос отдельному агенту пока не запускали.
- Добавили радар соседей отдельным разделом в HTML: все наблюдения, движения монет и объяснения видны рядом с основным анализом, но не смешиваются с ним. Устаревшие или недоступные результаты явно помечаем.
- Вынесли радар соседей в отдельную вкладку: выбранные агентом монеты идут первыми, у каждого кандидата виден график сравнения с его соседями. Основной анализ не изменили.
- Проверили на сохранённых данных: 27 наблюдений, 15 отмечены агентом; для всех кандидатов и лидеров есть история для графиков. Проверки прошли без нового запроса агенту.
- Собрали справочник кратких описаний всех 250 монет со ссылками на источники; отдельно уточнили новые проекты и неоднозначные тикеры.
- В обеих вкладках отчёта показываем краткое описание монеты и ссылки на источники перед графиком; оценки и отбор не меняли.
- Добавили автоматическое пополнение справочника для новых монет: старые описания сохраняем, а недоступные пробуем получить при следующем запуске, не мешая анализу.
- Проверили пополнение на реальных данных: добавились PHA и ONG, прежние 250 описаний сохранились без изменений; повторный запуск ничего не меняет.
- Дали агенту поиск и чтение источников для новых монет: он уточняет проект, составляет описание и сохраняет ссылки только на прочитанные страницы; прежние записи не трогает.
- Проверили новый поиск на Phala: агент нашёл официальную документацию и составил описание со ссылками; сохранённый справочник при проверке не меняли.
- Основной график отчёта теперь открывается на 7 дней; ручное переключение периодов осталось прежним.
- Ближайший шаг: посмотреть новую вкладку и обсудить, какие расхождения действительно полезны для наблюдения.
- Затем вернуться к строгости условий устойчивой силы, исключению поздних движений и определению сильного движения.

# План

- Утвердили набор признаков и решили собирать данные раз в час с Binance USDT perpetual.
- Собрали и утвердили список монет, за которыми будем следить.
- Загрузили для этих монет полную историю всех нужных данных.
- Добавили отдельную загрузку общей капитализации и основных сегментов рынка.
- Зафиксировали единое время сбора, чтобы все ряды заканчивались одним часом.
- Рассчитали все признаки и собрали компактный профиль для каждой монеты.
- Добавили предварительный отбор, который сохраняет разные типы сильных сигналов и сокращает список монет.
- Собрали компактную и понятную таблицу признаков для агента.
- Подключили официальный GitHub Copilot SDK для работы с агентом.
- Подготовили промпт, который оценивает вероятность сильного движения, выделяет пять самых интересных монет и возвращает понятный JSON.
- Для анализа выбрали GPT-5.6 Sol со стандартным контекстом и средним уровнем рассуждения.
- Добавили седьмой шаг: агент получает подготовленные признаки и при необходимости уточняет их по сырым данным.
- Точные значения признаков теперь подставляются автоматически, поэтому округления в ответе агента не ломают анализ.
- В список лучших кандидатов добавили прямые ссылки на их графики TradingView.
- В временных исключениях теперь сохраняем, каких данных не было и по какой причине.
- Дальше: повторно запустить анализ на реальных данных и проверить результат.
